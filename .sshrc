function tabc() {
  NAME=$1; if [ -z "$NAME" ]; then NAME="Solarized Light ansi"; fi
  osascript -e "tell application \"Terminal\" to set current settings of front window to settings set \"$NAME\""
}
